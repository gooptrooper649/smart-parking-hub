## Packages
leaflet | Core map library
react-leaflet | React bindings for Leaflet
@types/leaflet | TypeScript definitions for Leaflet
lucide-react | Icons
clsx | Class merging
tailwind-merge | Class merging
uuid | For generating unique IDs
@types/uuid | Types for uuid

## Notes
- Using react-leaflet for map rendering. Ensure Leaflet CSS is imported.
- Application state relies entirely on LocalStorage as requested, bypassing backend API.
- Custom ContextMenu implementation over the map for reliable positioning.
